# Frontend (Planned)

前端已初始化为 Vue 3 + TypeScript + Vite 最小联调页。

## 启动

```bash
npm install
npm run dev
```

## 当前功能

- 路由页面：
  - `/login`
  - `/competitors`
  - `/activities`
  - `/alerts`
- 已接通接口：
  - `/api/v1/auth/login`
  - `/api/v1/competitors`
  - `/api/v1/activities`
  - `/api/v1/activities/calendar`
  - `/api/v1/alert-rules`
  - `/api/v1/notifications`
